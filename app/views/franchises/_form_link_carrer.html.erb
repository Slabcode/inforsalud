<%= form_with(url: link_franchise_path(franchise), local: true) do |form| %>
  <% if franchise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(franchise.errors.count, "error") %> prohibited this franchise from being saved:</h2>

      <ul>
      <% franchise.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :carrer %>
    <%= form.select :carrer, options_from_collection_for_select(@carrers, :id, :name) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
