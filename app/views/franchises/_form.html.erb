<%= form_with(model: franchise, local: true) do |form| %>
  <% if franchise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(franchise.errors.count, "error") %> prohibited this franchise from being saved:</h2>

      <ul>
      <% franchise.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :franchise_name %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address, id: :franchise_address %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :franchise_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
