<%= stylesheet_link_tag 'admins', media: 'all', type:"text/css" %>

<section class="main">	
	<div class="login">
		<div class="logo">
			<%= image_tag "logo_inforsalud.jpg", :class => "logo img-responsive" %>
		</div>

		<h2 class="title">
			<%= I18n.t('authy_verify_installation_title', {:scope => 'devise'}) %>
		</h2>
		
		<%= verify_authy_installation_form do %>
		<div class="form-content">
			<legend><%= I18n.t('submit_token_title', {:scope => 'devise'}) %></legend>
			<%= text_field_tag :token, "", :autocomplete => :off, :placeholder => "Ingresa aquí el Token", :class => "authy-token", :id => 'authy-token' %>
			
			<div class="actions token">
				<%= authy_request_sms_link %>
				<%= submit_tag I18n.t('enable_my_account', {:scope => 'devise'}), :class => 'btn' %>
			</div>
				
		</div>
		
		<% end %>

	</div>
</section>
