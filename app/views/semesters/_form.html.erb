<%= form_with(model: semester,url: carrer_semesters_path(@carrer), local: true) do |form| %>
  <% if semester.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(semester.errors.count, "error") %> prohibited this semester from being saved:</h2>

      <ul>
      <% semester.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :semester_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :semester_description %>
  </div>

  <div class="field">
    <%= form.label :number %>
    <%= form.number_field :number, id: :semester_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
